/* Firas Apps — the live backend the packaged app talks to for AI / auth / chat.
   Set this to your deployed site URL (e.g. "https://firas-ai-xxxx.netlify.app").
   Left as a placeholder until you provide it; while it's a placeholder the app
   behaves same-origin (fine for local dev). */
window.FIRAS_API_BASE = "https://firasai.netlify.app";
window.FIRAS_APP = true;
